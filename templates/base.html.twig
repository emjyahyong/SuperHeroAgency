<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Agence des Super Héros{% endblock %}</title>

    <!-- Lien vers la feuille de style principale -->
    <link rel="stylesheet" href="{{ asset('assets/styles.css') }}">
    {% block stylesheets %}{% endblock %}
</head>
<style>
    /* Styles généraux */
    body {
        margin: 0;
        padding: 0;
        font-family: 'Arial', sans-serif;
        background-color: #f4f6f9; /* Fond légèrement gris-bleu clair */
        color: #333; /* Texte sombre pour un bon contraste */
    }

    header {
        background-color: #007BFF; /* Bleu vif pour la barre de navigation */
        padding: 1rem 2rem;
        border-bottom: 4px solid #28a745; /* Bordure verte douce pour contraste */
    }

    .navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .logo a {
        color: #fff; /* Blanc pour le logo */
        font-size: 2rem;
        font-weight: bold;
        text-decoration: none;
        letter-spacing: 2px;
    }

    .nav-links {
        list-style: none;
        display: flex;
        gap: 1.5rem;
    }

    .nav-links li a {
        color: #fff; /* Blanc pour les liens */
        text-decoration: none;
        font-size: 1.1rem;
        font-weight: 600;
        transition: color 0.3s ease;
    }

    .nav-links li a:hover {
        color: #28a745; /* Vert pour le survol */
    }

    main {
        padding: 2rem;
    }

    /* Styles pour les boutons */
    .btn {
        padding: 0.5rem 1.2rem;
        font-size: 1rem;
        font-weight: 600;
        text-decoration: none;
        border-radius: 0.25rem;
        transition: all 0.3s ease;
    }

    .btn-primary {
        background-color: #007bff;
        color: white;
    }

    .btn-primary:hover {
        background-color: #0056b3;
    }

    .btn-success {
        background-color: #28a745;
        color: white;
    }

    .btn-success:hover {
        background-color: #218838;
    }

    .btn-warning {
        background-color: #ffc107;
        color: black;
    }

    .btn-warning:hover {
        background-color: #e0a800;
    }

    .btn-danger {
        background-color: #dc3545;
        color: white;
    }

    .btn-danger:hover {
        background-color: #c82333;
    }

    .btn-sm {
        padding: 0.25rem 0.75rem;
        font-size: 0.875rem;
    }

    /* Section Hero */
    .hero-section {
        background: linear-gradient(135deg, #f4f6f9 30%, #007bff 100%); /* Dégradé subtil */
        height: 70vh;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #333;
        text-align: center;
    }

    .hero-content {
        background-color: rgba(255, 255, 255, 0.85); /* Fond translucide */
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.15);
    }

    .hero-content h1 {
        font-size: 3.5rem;
        margin-bottom: 1rem;
        color: #007bff; /* Bleu vif pour le titre */
    }

    .hero-content p {
        font-size: 1.5rem;
        margin-bottom: 2rem;
        color: #333;
    }

    .cta-button {
        background-color: #28a745; /* Vert pour le CTA */
        padding: 0.75rem 1.5rem;
        color: white;
        text-transform: uppercase;
        font-weight: bold;
        border-radius: 30px;
        transition: background-color 0.3s ease;
        box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.1);
    }

    .cta-button:hover {
        background-color: #218838;
    }

    footer {
        background-color: #007bff; /* Bleu vif pour le pied de page */
        color: white;
        text-align: center;
        padding: 1rem;
        margin-top: 3rem;
    }

    footer p {
        font-size: 1rem;
        margin: 0;
    }

    /* Amélioration du formulaire */
    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-control {
        width: 100%;
        padding: 0.75rem;
        font-size: 1rem;
        border-radius: 0.25rem;
        border: 1px solid #ccc;
        box-sizing: border-box;
    }

    .form-check-input {
        margin-right: 0.5rem;
    }

    /* Table styles */
    .table {
        width: 100%;
        margin-top: 2rem;
        border-collapse: collapse;
    }

    .table th, .table td {
        padding: 1rem;
        text-align: left;
        border-bottom: 1px solid #ddd;
    }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: #f9f9f9;
    }

    .table th {
        background-color: #007bff;
        color: white;
    }

    .table td {
        color: #555;
    }

</style>

<body>
    <header>
        <nav class="navbar">
            <div class="logo">
                <a href="{{ path('home') }}">SuperHero Agency</a>
            </div>
            <ul class="nav-links">
                <li><a href="{{ path('home') }}">Accueil</a></li>
                <li><a href="{{ path('app_super_hero_index') }}">Super Héros</a></li>
                <li><a href="#">Équipes</a></li>
                <li><a href="#">Missions</a></li>
            </ul>
        </nav>
    </header>

    <main>
        {% block body %}{% endblock %}
    </main>

    <footer>
        <p>&copy; {{ "now"|date("Y") }} SuperHero Agency - Tous droits réservés.</p>
    </footer>

    {% block javascripts %}{% endblock %}
</body>
</html>
